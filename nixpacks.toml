[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt",
    "pip install -e ."
]

[start]
cmd = "python scripts/run_mainnet_stream.py"
